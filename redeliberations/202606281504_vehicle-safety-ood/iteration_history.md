## 迭代第 1 轮

1. **问题描述**：DS-07 InterventionService 的通用 RiskLevel→干预映射与 Scene 1 具体场景行为描述不一致——DS-07 定义 L2→语音提醒+环境调节，但 Scene 1 中疲劳 L2→氛围灯变橙。干预策略实际应按 AlertType+RiskLevel 二维映射，而非单一 RiskLevel 映射。
   - 所在位置：DS-07（§3.4）vs Scene 1（§四）
   - 严重程度：严重
   - 改进建议：DS-07 改为「AlertType+RiskLevel→干预指令集合」的二维映射，或明确说明通用映射仅为示意，实际映射由各场景行为契约定义。DS-07 与 Scene 1 必须一致。
2. **问题描述**：路怒解除事件的语义在现有类型系统中无法表达——Scene 3 要求产出 RiskDeterminedEvent 表示"风险解除"，但 RiskLevel 枚举仅含 {L1_HINT, L2_WARNING, L3_CRITICAL}，AlertType 枚举仅含 {FATIGUE, DISTRACTION, ROAD_RAGE}，均无可表示"已解除"的取值。
   - 所在位置：Scene 3（§四）vs VO-01 RiskLevel / VO-02 AlertType（§3.3）
   - 严重程度：严重
   - 改进建议：二选一：(a) 为 RiskLevel 增加 RESOLVED 枚举值；(b) 单独定义 RiskResolvedEvent 领域事件。方案 (a) 更简洁，需同步修正 VO-01 取值说明。
3. **问题描述**：分心检出缺少独立的行为契约场景——DS-03 描述了判定逻辑，但 §四 无分心检出从感知输入→判定→告警下发的端到端时序和交互契约，0.5s 告警时延约束的验证依据不足。
   - 所在位置：§四 关键行为契约
   - 严重程度：一般
   - 改进建议：在 §四 新增分心检出与告警场景，明确 SensorReading→DS-03 判定→RiskDeterminedEvent→InterventionService 告警的完整链路与时延约束。
4. **问题描述**：OTA 升级管理缺少行为契约场景——DS-15 列出六项职责，但 §四 无对应场景，状态机阶段、状态转换触发条件、断点续传重试策略等均未明确。
   - 所在位置：DS-15（§3.4）、§四 关键行为契约
   - 严重程度：一般
   - 改进建议：在 §四 新增 OTA 固件升级全流程场景，明确状态机阶段、状态转换触发条件、失败回滚策略、断点续传重试语义。
5. **问题描述**：VehicleStateBuffer 端口接口契约未定义——决策 14 声明了该接口，但整个设计文档未给出方法签名或能力契约（参数化方式、返回值类型、错误语义），阻塞上下游并行开发。
   - 所在位置：决策 14（§七）、DS-06（§3.4）
   - 严重程度：一般
   - 改进建议：在 VO-09 或决策 14 处补充 VehicleStateBuffer 接口的核心方法签名，至少包含方法名、输入参数类型、返回类型、错误语义。
6. **问题描述**：InterventionInstruction 和 RescueReport 两个值对象未列入 §3.3 值对象目录——DS-07 和 DS-12 分别输出这两个值对象，但 §3.3 仅列至 VO-11，缺少正式类型定义。
   - 所在位置：DS-07、DS-12 vs §3.3 值对象目录
   - 严重程度：一般
   - 改进建议：在 §3.3 补充 VO-12 InterventionInstruction 和 VO-13 RescueReport，明确各自字段结构、不变式约束和类型形态。
7. **问题描述**：行为契约场景覆盖不完整——15 个领域服务中有 5 项无场景覆盖（分心检出、OTA 升级、报告生成与导出、远程车窗控制、看板钻取），覆盖率约 67%，部分功能（15s 报告 SLA、0.5s 分心告警时延）带有显式时序约束但无场景契约验证。
   - 所在位置：§四 关键行为契约 vs §三 服务定义
   - 严重程度：一般
   - 改进建议：在 §四 补全上述功能域的行为契约场景，至少覆盖分心检出、OTA 升级和报告生成（三者带有显式时序约束）。


## ������ 2 ��

1. **��������**��BR-05 ���ֹ�ʽ�뱨������ġ���ɲ/�����ٴ�����ȫ��·��������Դ����AlertType ö���޶�Ӧȡֵ��SafetyAlertEvent �澯�����б��޴��ࡢ����������������޶�Ӧ�¼���ͳ���ֶΣ�VehicleStateSnapshot ������ײʱ���ɻ�����ʩ ring buffer ��ȡ�����г�ȫ�̳������������� ScoringService �������޿ɼ������롢�����ָ��������Դ��
   - ����λ�ã�DS-09 ScoringService����3.4 Э��-���룩������ 5 ���� 2��DS-11 ReportGenerationService����3.4����VO-02 AlertType����3.3����E-01 SafetyAlertEvent����3.2��
   - ���س̶ȣ�һ��
   - �Ľ����飺���뼱ɲ/����������ģ��Դ����ѡһ����(a) ������ʻ��Ϊ���������񣬻��ڼ��ٶ�ͨ�������ж���������������Ӧ�澯�¼�/ͳ�ƣ���Ϊ SafetyAlertEvent/AlertType �������м����ֶΣ�(b) ��ȷ��ָ���ɻ�����ʩ���� Trip ά�ȳ����ۼƵĶ������������ṩ������ DS-09/DS-11 ������Լ��������Դ�����ͨ����� �� ���� �� ���� Trip ͳ�� �� �� ScoringService/ReportGenerationService ���ѡ�������·��

2. **��������**��Զ�̿س�/�Խ�/�������Ƶġ�����������֤��Ϊ req ��ʽ��ȫ����req ��������3.4��������Ȩ����δ�轨ģ������ 11 ��ȨУ��������� AccountRole �� Permission У�鳵��������Ȩ��δ���ֳ���·����ǿ�ƵĶ���������֤���ڣ�DS-08��VO-07 Permission ��δ����ð�ȫ�ſء���Ȩ������������㽨ģȴ��©���еĶ�����֤���裬���ɶ�����ȫ������Ӧ����֡�
   - ����λ�ã����� 11�����ģ���DS-08 PermissionService����3.4����VO-07 Permission����3.3��
   - ���س̶ȣ�һ��
   - �Ľ����飺�ڳ��� 11 ��ȨУ�鲽�衢DS-08 �� ���尲ȫԼ���в��䡸�������Զ�̲������Խ�/��Ƶ/����/����������ͨ������������֤��ʧ�ܷ�����Ӧ Result ���󡹣�����ȷ BR-06 ��Σʧ���Զ�����·��Ϊ������������ req ��������3.4 һ�£���

3. **��������**��req ��3.1 ��ȷ�����ġ����ź�������ͷ����֪Դ�ڸ�֪������δ����ϡ�SensorReading �� DS-01 �����ö�١��Ĵ��֪ͨ����DMS �Ӿ��������������������������ײ��״������ req ���������֪Դ��һ�£������ˡ���Ƶ��ء�Ȩ�޵Ļ�������Դ��������޶�Ӧ˵����
   - ����λ�ã�VO-11 SensorReading����3.3����DS-01 Э��-���루��3.4��
   - ���س̶ȣ���΢
   - �Ľ����飺�ڸ�֪ͨ��˵���в�����ź�������ͷ��ΪӰ��Դ������ȷ������ƵӰ��Դ����������ʩ�㡢����㲻��ģ��ע����Դ���Ապ��� req ��3.1 �Ķ�Ӧ��ϵ��

4. **��������**��Driver���ۺϷ������֡����Եĸ�����Դδ�պϡ�AR-02 ���� Driver �ۺϹ������ۺϷ������֡����� DS-09 ���� TripScore���г̼��������ڼ����֣�VO-05�������δ˵�� Driver ����������һӳ�䡢��˭�ں�ʱд�� Driver �ۺϣ���ֵ/������·���ա�
   - ����λ�ã�AR-02 Driver����3.1����DS-09 ScoringService����3.4��
   - ���س̶ȣ���΢
   - �Ľ����飺��ȷ Driver.�ۺϷ������ֵ�ȡֵ�ھ�����ȡ������ڼ����֣�����´����㣨�� ScoringService ����������ɺ��¼���д Driver �ۺϣ������� AR-02 ��ɾ�����ֶθ��ɲ�ѯͶӰ���㡣

## 迭代第 3 轮

1. **问题描述**：DS-05 LifeDetectionService 的触发源"熄火落锁事件"未在领域事件表中建模，导致触发路径在设计层面悬空——消费方声称订阅了一个尚未定义的事件，BR-02 活体遗留检测的完整判定链路从第一步起断裂
   - 所在位置：DS-05 协作-触发（§3.4）与 §3.5 领域事件一览表之间的不一致
   - 严重程度：一般
   - 改进建议：在 §3.5 领域事件表新增类似 VehicleIgnitionOffLockedEvent（携带 Vehicle 标识、时间戳）的事件类型，DS-05 的触发说明引用该事件名称，Vehicle 聚合的状态变更应产出此事件。或者若认为熄火落锁是纯基础设施层信号，则在 DS-05 中明确声明触发机制为"基础设施层信号"以消除矛盾表述

2. **问题描述**：DS-08 PermissionService 的 L3 风险持续 60 秒计时状态归属未明确——领域服务在设计中保持无状态，但 DS-08 需要为每个 Driver 维护 L3 计时器以判断是否超过 60 秒阈值，状态应存在何处、由谁创建和清理、多 Driver 并发判定时如何隔离均未说明
   - 所在位置：DS-08 PermissionService（§3.4）与 Scene 7（§四）
   - 严重程度：一般
   - 改进建议：二选一：(a) 参照 DS-05 的 DetectionWindow 模式，将 L3 计时信息封装为 L3DurationTracker 值对象，由边缘侧会话上下文持有并作为输入参数传入 DS-08，保持 DS-08 纯函数语义；(b) 在 DS-08 描述中显式声明"以单会话级状态维护 per-Driver L3 计时器，状态生命周期与 Trip 会话一致"并说明清理时机

3. **问题描述**：摄像头物理遮挡的检测源模糊，未指定是哪个感知通道检测，可能与 BR-08 传感器故障路径产生歧义；遮挡解除后的权限恢复路径缺失，未明确是否自动恢复或重新计时
   - 所在位置：Scene 7 步骤 4（§四）与 DS-08（§3.4）
   - 严重程度：轻微
   - 改进建议：(a) 明确物理遮挡的检测机制，说明 DS-14 如何区分"传感器故障"与"物理遮挡"并产生不同的事件/信号；(b) 补充遮挡解除后的权限恢复路径，明确是否自动恢复、是否重新计时

4. **问题描述**：DriverScoreUpdatedEvent 的消费方被描述为"Driver 聚合"或"Driver 聚合的仓储"，与 DDD 分层惯例存在张力——聚合不直接作为事件消费者，仓储不应承担事件订阅/消费角色
   - 所在位置：§3.5 领域事件表 DriverScoreUpdatedEvent 条目与 DS-09 ScoringService 方案 (a)（§3.4）
   - 严重程度：轻微
   - 改进建议：统一表述，明确 DriverScoreUpdatedEvent 的消费方为一个轻量领域服务（如 DriverScoreUpdateService），由该服务通过 DriverRepository 更新 Driver 聚合

5. **问题描述**：VO-13 RescueReport 中"生命体征摘要"声称来源为"Driver 实时生理监测"，但 DS-12 EmergencyRescueService 仅查询 DriverHealthProfile（档案级数据），未提及从 Trip 聚合的 PhysiologicalSnapshot 提取实时数据，数据提取路径未对接
   - 所在位置：VO-13 RescueReport（§3.3）、DS-12 EmergencyRescueService（§3.4）、Scene 4 步骤 5（§四）
   - 严重程度：轻微
   - 改进建议：在 DS-12 的协作-输入中补充"查询 Trip 聚合的最新 PhysiologicalSnapshot 以提取实时生命体征摘要"，在 VO-13 中标注数据来源为 Trip 聚合中的 PhysiologicalSnapshot

6. **问题描述**：DS-16 DriverStatusBroadcastService 跨端推送的中转路径未描述——边缘侧产出快照后，如何经推送通道到达云端家属端（直接推送还是依赖通用上报链路？），≤2s 端到端时延约束下两级网络跃点的分配预算未说明
   - 所在位置：DS-16 DriverStatusBroadcastService（§3.4）、Scene 10b（§四）、§6.2
   - 严重程度：轻微
   - 改进建议：在 DS-16 协作说明或 §6.2 中补充边缘侧→云端推送服务→家属 APP 的中转路径简图，明确不依赖 Trip 同步上报链路，并说明 ≤2s 端到端时延的分配预算


## 迭代第 4 轮

1. **问题描述**：§3.5 领域事件章节存在结构错误——DS-18 领域服务被误置于事件章节下，且 `### 3.5 领域事件` 标题出现两次（第 613 行与第 627 行），导致文档结构混乱、章节编号唯一性缺失，下游引用产生歧义
   - 所在位置：第 613 行（DS-18 及其 `### 3.5 领域事件` 标题）与第 627 行（重复的 `### 3.5 领域事件` 标题）
   - 严重程度：严重
   - 改进建议：(a) 将 DS-18 DriverScoreUpdateService 移至 §3.4 领域服务章节末尾；(b) 删除第 613 行的 `### 3.5 领域事件` 标题；(c) 确保 §3.5 标题仅在第 627 行出现一次
2. **问题描述**：Trip:Driver=1:1 基数约束与 L3DurationTracker 的多 Driver key 映射假设在设计内部多处自相矛盾——AR-01 第 78 行声明 Trip:Driver=1:1，但 AR-01 第 81 行、VO-17 第 364/371 行、DS-08 第 489 行均引入 per-Driver key 映射 / 多 Driver 并发判定表述，与 1:1 约束直接冲突
   - 所在位置：AR-01 第 78 行（Trip:Driver=1:1）⇔ AR-01 第 81 行、VO-17 第 364/371 行、DS-08 第 489 行
   - 严重程度：严重
   - 改进建议：方案 (a)（推荐，与 req_v4 一致）：维持 Trip:Driver=1:1，从 AR-01 第 81 行、VO-17 第 364/371 行、DS-08 第 489 行一并移除 per-Driver/multi-Driver 表述，改为简单持有语义；方案 (b)：在 AR-01 第 78 行显式放宽基数并说明理由。无论取哪一方案，四处必须表述一致
3. **问题描述**：领域事件总线仅声明"需自定义实现"（§6.2 第 884–887 行），未定义接口契约——事件注册/发布/订阅的 API 形态、线程模型、错误语义、outbox 投递保证级别均缺失，所有事件发布/订阅代码无接口契约可依
   - 所在位置：§6.2（第 884–887 行）、§3.4 各领域服务的"协作"小节、决策 10
   - 严重程度：一般
   - 改进建议：在 §3.5 或独立决策中定义领域事件总线核心接口契约，至少包含：(a) 事件发布接口签名；(b) 同步消费注册机制；(c) 异步消费生命周期管理；(d) outbox 表结构与投递保证级别
4. **问题描述**："会话上下文"被 DS-01、DS-16、DS-05 多处引用且持有内容已在 §6.2 第 880 行列举，但仍未升格为正式建模的领域组件——其生命周期、与 Trip 聚合的归属关系、线程安全语义均未定义；且 L3DurationTracker 在"归 Trip 聚合持有"与"归会话上下文持有"之间存在双重归属张力
   - 所在位置：DS-01（第 383 行）、DS-16（第 593 行）、DS-05（第 434 行）；持有内容见 §6.2 第 880 行；张力见 VO-17（第 364–371 行）、DS-08（第 489 行）
   - 严重程度：一般
   - 改进建议：(a) 在 §三 或决策章节新增对"边缘侧会话上下文"的正式定义，明确其为 Trip 聚合的运行时投影/缓存还是独立组件；(b) 厘清其持有内容与 Trip 聚合的关系，消除 L3DurationTracker 双重归属矛盾；(c) 明确其生命周期边界
5. **问题描述**：外部系统（SMN/SparkRTC/IoTDA/120 救援中心/OTA 下发）的领域层端口契约缺失——DS-12/DS-15/DS-16 以自然语言描述依赖，均未声明领域层的依赖端口接口（对比 VehicleStateBuffer 和 DrivingBehaviorTrackingPort 已有完整端口契约，处理标准不一致）
   - 所在位置：DS-12（第 540–546 行）、DS-16（第 593–597 行）、DS-15（第 577–584 行）、§6.2（第 889 行）
   - 严重程度：一般
   - 改进建议：参照决策 14/17 的端口建模方式，定义：(a) NotificationPort（推送）；(b) RescueReportPort（救援上报）；(c) MediaSessionPort（音视频对讲）；(d) OTADeliveryPort（OTA 升级包下发）。如认为这些端口属基础设施层，需在决策中统一说明理由并描画替代协作机制
6. **问题描述**：急刹/急加速的检出阈值未定义来源——VO-16 第 354 行仅写"超过阈值"而无数值，阈值是领域规则还是基础设施调优参数未予区分，实现者将自行杜撰阈值，影响评分一致性
   - 所在位置：VO-16 DrivingBehaviorCounters（第 346–358 行）、DS-17（第 601–609 行）、决策 17
   - 严重程度：轻微
   - 改进建议：(a) 在 VO-16 或 DrivingBehaviorTrackingPort 中声明阈值作为端口参数/领域配置项；(b) 或明确标注阈值为基础设施层配置，但注明"不同阈值将导致评分不一致，验收时需固定阈值"
7. **问题描述**：救援机构（AccountRole.RESCUE？）角色未建模——req_v4 §二第 41 行列救援机构为五类参与者之一，§3.6 第 100–101 行明确其权限，但 VO-14 AccountRole 第 323 行仅含 FAMILY/MANAGER 两枚举值，救援机构无对应角色定义
   - 所在位置：VO-14 AccountRole（第 323 行）、AR-04 SystemAccount（§3.1）、DS-12（第 540 行）
   - 严重程度：轻微
   - 改进建议：(a) 如救援机构确需建模为 SystemAccount，为 AccountRole 增加 RESCUE 枚举值并在 DS-12 中实现类型安全权限分支；(b) 如走临时令牌/一次性授权，则在 DS-12 或决策中明确说明此假设以消除歧义
8. **问题描述**：家属通知偏好被引用但未形式化为可配置字段/值对象——AR-04 第 129 行引用"根据角色与通知偏好决定是否推送"，req_v4 §3.4 第 83 行要求"可设置并接收轻/中/重度不同等级"，但 AR-04 与 VO-07 Permission 均未包含通知偏好字段或关联值对象，缺少 `Set<RiskLevel>` 风险等级订阅粒度，无可落地过滤契约
   - 所在位置：AR-04 SystemAccount（第 122、129 行）、VO-07 Permission（§3.3）、DS-16 与告警推送协作描述
   - 严重程度：轻微
   - 改进建议：在 AR-04 SystemAccount 或关联值对象中新增 `NotificationPreference` 值对象（含订阅的风险等级集合），并在 DS-16 和告警推送协作说明中补充"按家属偏好按等级过滤后推送"的可落地语义


## 迭代第 5 轮

1. **问题描述**：RoadRageVoiceRecord (E-02) 无明确所有者聚合，E-02 第 156 行声称"通过 SafetyAlertEvent 的一致性边界管理"（暗示归属 Trip 聚合），但决策 6 第 1024–1026 行明确排除其属于 Trip 聚合，形成归属矛盾。同时 PrivacyProtectionService（领域服务）直接管理该实体生命周期，违反 DDD 实体必须通过其所属聚合根仓储进行持久化的原则。
   - 所在位置：E-02 第 156 行；决策 6 第 1024–1026 行；DS-13 第 571–573 行
   - 严重程度：一般
   - 改进建议：二选一——(a) 将 RoadRageVoiceRecord 独立为聚合根，PrivacyProtectionService 通过 RoadRageVoiceRecordRepository 操作其生命周期；(b) 将 RoadRageVoiceRecord 正式归属 Trip 聚合，写侧通过 Trip 聚合根一致性边界创建/更新，读侧以 CQRS 投影屏蔽存储位置差异。

2. **问题描述**：DS-15 OTAUpdateService 第 594–599 行声称"升级包的状态机由本服务维护"，但领域服务在 DDD 中应保持无状态。OTA 升级状态涉及持久状态（待下发→传输中→……→完成/回滚）和断点续传偏移量等，设计未说明该状态归属于哪个聚合，实现者将面临状态持久化、事务边界和并发控制的技术空白。
   - 所在位置：DS-15 第 594–599 行；场景 9 第 832–843 行
   - 严重程度：一般
   - 改进建议：将 OTA 升级状态建模为 Vehicle 聚合的内部值对象（如 OTAUpgradeStatus，包含当前阶段、进度、偏移量等），或单独建模为 OTAUpgradeSession 聚合根。DS-15 保持无状态语义。

3. **问题描述**：Vehicle 聚合 (AR-03) 第 108 行属性列表缺少当前固件版本字段，但场景 9 第 834 行和 DS-15 第 597 行均要求 OTAUpdateService 进行版本比对，导致领域层无 Vehicle 当前版本可读，比对操作无法落地。
   - 所在位置：AR-03 Vehicle 第 108 行；场景 9 步骤 1 第 834 行；DS-15 第 597 行
   - 严重程度：一般
   - 改进建议：在 AR-03 Vehicle 属性列表中增加当前固件版本字段（以 VO-08 OTAVersion 表达）；DS-15 从 Vehicle 聚合读取当前版本与目标版本比对；升级完成后通过 Vehicle 聚合更新固件版本号。

4. **问题描述**：InterventionInstruction (VO-12) 第 293 行的指令类型以"建议以 enum 表达有限指令集合，如……等"描述，其中"建议"暗示非强制契约、"等"暗示未穷举。DS-07 二维映射表第 471–475 行以自然语言描述干预动作，未能逐条映射到具体 InstructionType 枚举值，导致基础设施层无法确信需要处理哪些指令类型及其参数结构。
   - 所在位置：VO-12 第 293 行；DS-07 第 471–475 行、第 481 行
   - 严重程度：一般
   - 改进建议：(a) 将指令类型从"建议 enum"改为正式定义的 InterventionInstructionType 枚举值，穷举所有指令种类；(b) DS-07 二维映射中逐条补充到 InstructionType + 参数映射的明确对应。

5. **问题描述**：SafetyAlertEvent (E-01) 的创建者在设计中未显式指定——场景 1 第 727 行和场景 8 第 826 行均以被动语态"生成 SafetyAlertEvent"表述，DS-01 第 403–404 行仅产出 RiskDeterminedEvent/RiskResolvedEvent。E-01 第 141 行暗示应通过 Trip 聚合根创建，但 RiskDeterminationService 不持有 Trip 引用或仓储，形成创建者悬空。
   - 所在位置：场景 1 第 727 行；场景 8 第 826 行；E-01 第 141 行；DS-01 第 403–404 行
   - 严重程度：一般
   - 改进建议：推荐新增轻量领域服务 AlertPersistenceService，订阅 RiskDeterminedEvent 等事件，通过 TripRepository 创建 SafetyAlertEvent 后发出 AlertTriggeredEvent，将判定与告警实体持久化解耦。

6. **问题描述**：分心检出中"累计达 3 秒"的时间窗口未定义。DS-03 第 420 行定义"视线偏离前方持续/累计达 3 秒即判定为分心成立（L2）"，但未指定累计的滑动窗口时长。无窗口将导致累计值随行程单调递增，长期行程中所有驾驶员均被误判为分心。
   - 所在位置：DS-03 第 420 行；req_v4 第 132 行
   - 严重程度：轻微
   - 改进建议：在 DS-03 中补充累计窗口假设（如"最近 60 秒滑动窗口内累计偏离 >3 秒"），标注为需与需求方确认的推断；或将窗口定义为可配置参数。

7. **问题描述**：Decision 20 端口方法命名 detectHardBraking(signal, threshold) / detectHardAcceleration(...) 暗示"领域层调用端口执行检测"，与 VO-16 第 356 行"基础设施层检测→通知领域层"的数据流方向相反，命名不一致将导致实现者对驱动方产生误读。
   - 所在位置：VO-16 第 356 行；DS-17 第 621 行；决策 20 第 1212–1214 行
   - 严重程度：轻微
   - 改进建议：统一命名方向——若保持"基础设施检测→通知领域"，端口方法应改为事件回调风格如 onHardBrakingDetected(event): Unit；若保持 detect* 命名，需将检测职责归入领域层并同步修改 VO-16 和 DS-17。

8. **问题描述**：DS-06 EmergencyResponseService 在决策 13 第 1067–1074 行被归类为"事件触发型判定服务"，但其判定依赖"碰撞特征冲击 AND 生理监测显示心率骤停或意识丧失 >10 秒"，要求持续接收生理数据流。分类偏差可能误导实现者为 DS-06 设计错误的数据接收模型。
   - 所在位置：决策 13 第 1071–1074 行；DS-06 第 453–462 行；DS-01 第 401 行
- 严重程度：轻微
  - 改进建议：将 DS-06 的分类修正为"以碰撞为触发条件、依赖持续生理数据缓冲的判定服务"，并在设计约束中补充需维护 ≥10s 的生理数据滚动缓冲区。


## 迭代第 6 轮

1. **问题描述**：PhysiologicalDataBuffer端口仅提及名称，未定义接口契约（方法签名、输入参数类型、返回类型、错误语义均缺失），导致DS-06（EmergencyResponseService）对生理数据回取无可编码的契约依据
   - 所在位置：DS-06（第477行）、决策13（第1110-1112行）
   - 严重程度：一般
   - 改进建议：参照决策14 VehicleStateBuffer的建模方式，补充PhysiologicalDataBuffer端口契约，至少包含：方法名、输入参数（TripId+TimeWindow）、返回类型（如Result<Array<PhysiologicalSnapshot>, BufferError>）、错误语义（如WindowNotCovered/BufferUnavailable）

2. **问题描述**：AR-01 Trip聚合协作关系中表述ScoringService负责将TripScore写回Trip聚合，但DS-09 ScoringService的职责描述与场景5均未提及该持久化操作，TripScore的持久化职责归属悬空
   - 所在位置：AR-01第84行 vs DS-09第532-548行 vs 场景5第819-826行
   - 严重程度：一般
   - 改进建议：二选一：(a)在DS-09协作中显式补充通过TripRepository写入操作；(b)新增TripScorePersistenceService订阅TripScoredEvent执行写回，参照DS-18模式。AR-01、DS-09、场景5三处必须表述一致

3. **问题描述**：Scene 6传感器故障场景仅覆盖SensorFailureEvent的处理路径，未涉及DS-14已分化的CameraOcclusionDetectedEvent/CameraOcclusionRemovedEvent物理遮挡检测路径
   - 所在位置：Scene 6（第830-837行）vs DS-14（第600-610行）
   - 严重程度：轻微
   - 改进建议：在Scene 6中补充物理遮挡检出与解除的处理分叉，或将物理遮挡端到端契约独立为Scene 6b，并在Scene 6标题下添加总述说明两条路径差异

4. **问题描述**：§5.4边界条件描述了驾驶员注销/账号删除时的清理动作以领域事件驱动，但§3.5领域事件一览表中缺少对应的DriveDeactivatedEvent或DriverAccountDeletedEvent事件类型
   - 所在位置：§5.4边界条件(2)（第963行）vs §3.5领域事件一览表（第676-693行）
   - 严重程度：轻微
   - 改进建议：在§3.5事件表中新增DriverDeactivatedEvent并为其指定消费方，或明确说明清理由应用层服务编排驱动而非领域事件

5. **问题描述**：NAVIGATE_TO_SHOULDER指令类型同时承载"建议减速"和"引导靠边"两个语义不同阶段的干预意图，基础设施层无法仅凭指令类型区分当前阶段
   - 所在位置：VO-12第294行、DS-07二维映射表第495行
   - 严重程度：轻微
   - 改进建议：二选一：(a)拆分为NAVIGATE_DECELERATION和NAVIGATE_TO_SHOULDER两阶段；(b)在参数映射中显式定义阶段标记参数

## 迭代第 7 轮

1. **问题描述**：场景 8 第 883–884 行连续出现两个步骤编号""6.""，后者应为""7.""，导致下游引用（如""场景 8 步骤 6""）无法唯一确定所指内容。
   - 所在位置：§四 场景 8，第 883–884 行
   - 严重程度：一般
   - 改进建议：将第 884 行的""6.""改为""7.""，并同步检查 §四 其余场景的步骤编号连续性。
2. **问题描述**：DS-10 FleetAnalyticsService 与 DS-11 ReportGenerationService 仅以自然语言描述职责，缺少形式化方法签名（参数类型、返回类型、错误语义），与 DS-06/DS-12/DS-15/DS-16 等已定义完整端口契约的处理标准不一致，下游消费者无法确定调用契约。
   - 所在位置：DS-10（§3.4，第 558–568 行）、DS-11（§3.4，第 569–577 行）
   - 严重程度：一般
   - 改进建议：为 DS-10 定义 getFleetFatigueDistribution() 和 drillDown(riskLevel: RiskLevel) 等方法契约及返回类型结构概要；为 DS-11 定义 generateReport(driverId: DriverId, timeRange: TimeRange) 方法契约，含 15s SLA 声明和错误语义。参照决策 19 的端口定义风格。
3. **问题描述**：AR-02 第 92–93 行描述 Driver 综合风险评分写回路径时，括号内""（或 ScoringService 自身直接操作 Driver 仓储）""与 DS-09/DS-18 已统一采用的 DS-18 DriverScoreUpdateService 消费路径矛盾，属此前 a_v4 修复的残留表述。
   - 所在位置：AR-02 第 92–93 行；对比 DS-09 第 546–547 行、DS-18 第 657–663 行
   - 严重程度：一般
- 改进建议：删除括号内备选路径，将表述改为""由 DriverScoreUpdateService（DS-18）消费该事件后通过 DriverRepository 更新""，与 DS-09/DS-18 保持一致。


## 迭代第 8 轮

1. **问题描述**：聚合根仓储（Repository）接口契约未定义——TripRepository、DriverRepository、RoadRageVoiceRecordRepository等仓储接口的方法签名、返回类型、错误语义均未形式化定义，阻塞编码实现
   - 所在位置：全文未定义——§3.4各领域服务中引用仓储；§6.2提及乐观锁但未定义仓储接口；§3.6事件总线契约已完整但仓储契约缺位
   - 严重程度：一般
   - 改进建议：(a) 为TripRepository、DriverRepository、SystemAccountRepository、RoadRageVoiceRecordRepository定义核心方法契约——findById、save的方法签名、返回类型（Result<T, PersistenceError>或Option<T>）、乐观锁冲突错误语义；(b) 或明确声明"仓储接口遵循DDD通用仓储模式，由基础设施层定义具体接口，领域层仅声明依赖"，以消除歧义并统一标准
2. **问题描述**：远程对讲/视频介入时的驾驶员声光提示未建模——req_v4 §3.4明确要求远程对讲/视频介入启动时必须声光提示驾驶员，但当前设计在FamilyAccessGrantedEvent消费方和场景7中均未体现驾驶员HMI声光提示的消费动作
   - 所在位置：§3.5领域事件表FamilyAccessGrantedEvent消费方（第706行）；DS-08协作（第529行）；§四场景7步骤2（第876行）
   - 严重程度：一般
   - 改进建议：(a) 在FamilyAccessGrantedEvent消费方中新增"HMI层（向驾驶员发出声光提示：对讲/视频已接通）"；(b) 在场景7步骤2中补充"同时通过HMI向驾驶员发出声光提示——告知远程对讲/视频已建立，并保留驾驶员物理遮挡权"


## 迭代第 9 轮

1. **问题描述**：VehicleRepository仓储接口未在§3.7中形式化定义，§3.1聚合根对照表中以注释代替契约，但DS-12（更新车门锁状态）、DS-15（固件版本读写、OTA状态变更）、DS-14（传感器自检状态更新）均需要对Vehicle聚合执行持久化操作
   - 所在位置：§3.7聚合根仓储接口契约、§3.1聚合根-仓储对照表
   - 严重程度：一般
   - 改进建议：在§3.7中补充VehicleRepository的方法契约，至少包含findById和save方法签名及乐观锁冲突语义
2. **问题描述**：DS-06 EmergencyResponseService承载碰撞失能判定的完整链路（碰撞信号→生理数据回取→车辆状态回取→失能判定→事件产出），但未获得任何方法签名，下游消费者无法确定方法名、参数类型、返回类型
   - 所在位置：DS-06 EmergencyResponseService（§3.4）
   - 严重程度：一般
   - 改进建议：参照DS-02~DS-05的接口定义风格，为DS-06补充核心方法签名，明确CollisionImpactSignal载荷结构和DeterminationError错误语义
3. **问题描述**：DS-15 OTAUpdateService驱动完整OTA升级状态机，协作涉及版本比对、传输进度处理、校验结果处理、刷写结果处理等多个独立操作，但没有任何方法签名，与DS-07（两个方法签名）、DS-14（三个方法签名）的契约完备性标准不一致
   - 所在位置：DS-15 OTAUpdateService（§3.4）
   - 严重程度：一般
   - 改进建议：参照DS-07/DS-14的接口契约风格，为DS-15补充核心方法签名（至少initiateUpgrade、handleTransferProgress、handleVerificationResult）
4. **问题描述**：DS-19明确"不产生AlertTriggeredEvent"但创建SafetyAlertEvent并持久化，与§3.5事件表中EmergencyActivatedEvent的消费方列有AlertPersistenceService、场景4步骤3的描述存在歧义——SafetyAlertEvent实体存在但无法通过常规告警查询路径检索到，形成半可见的"暗数据"
   - 所在位置：DS-19协作 vs §3.5事件表EmergencyActivatedEvent条目
   - 严重程度：一般
   - 改进建议：二选一：(a)碰撞失能告警仍需发出AlertTriggeredEvent使管理员可审计；(b)明确声明碰撞失能告警不走常规告警通知链路但走独立审计查询投影
5. **问题描述**：救援机构的远程解锁授权模型未形式化——需求明确要求"云端授权开启车门锁"和"调取驾驶员健康档案"，但设计文档中授权模型仅以自然语言存在，授权的生命周期、与AccountRole.RESCUE的关系、授权凭证建模方式均无形式化描述
   - 所在位置：DS-12 EmergencyRescueService（§3.4）、场景11（§四）、AR-04 SystemAccount（§3.1）
   - 严重程度：轻微
   - 改进建议：二选一：(a)补充救援机构授权建模（授权作为SystemAccount的Permission值对象或独立令牌，生命周期及校验流程）；(b)明确声明救援授权为外部一次性令牌，领域层仅校验有效性
6. **问题描述**：DomainEvent接口的aggregateId字段使用String类型，与决策8"聚合标识使用仓颉的struct类型"不一致，导致事件消费方需手动解析字符串、无法在编译期区分聚合根标识类型
   - 所在位置：§3.5.0 DomainEvent公共抽象
   - 严重程度：轻微
   - 改进建议：将aggregateId改为泛型或带类型标记的结构体（如AggregateId struct包含value和aggregateType枚举），或将DomainEvent改为泛型接口
7. **问题描述**：DS-09 ScoringService周期评分边界语义未定义——跨周期行程的归属规则、加权平均计算公式、计算时机（即时预计算还是按需计算）均缺失
   - 所在位置：DS-09 ScoringService（§3.4）
   - 严重程度：轻微
   - 改进建议：明确周期归属规则、加权公式、计算时机，标注可配置或需确认的参数
8. **问题描述**：§6.3并发策略表未覆盖OTA升级状态更新与传感器自检状态更新同时对Vehicle聚合执行的并发一致性场景
   - 所在位置：§6.3并发策略表、VO-19 OTAUpgradeStatus（§3.3）、DS-15/DS-14（§3.4）
   - 严重程度：轻微
   - 改进建议：在§6.3补充该并发策略条目，明确乐观锁冲突处理语义，或声明云端与边缘侧通过单向事件通信隔离

